// Toast configuration options
const toastConfig = {
  position: "top-right",
  toastClassName: "my-toast",
  bodyClassName: "my-toast-body",
  closeButton: false,
  autoClose: 1500, // Close the toast automatically
};

export default toastConfig;
